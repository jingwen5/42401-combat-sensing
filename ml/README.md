# ML Pipeline

This folder contains the machine learning pipeline for feature extraction and model development.

Workflow:
1. Prepare data (IMU, ECG, PPG signals)
2. Run feature_extractor.py to generate model inputs
3. Train models and save them as `.pkl` files
4. Use trained models for inference or evaluation
